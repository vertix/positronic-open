defaults:
  - _self_
  - mujoco_loaders: default

type: mujoco

mujoco:
  model_path: null
  camera_names: ['front_view']
  camera_width: 352
  camera_height: 192
  simulation_hz: 500
  observation_hz: 60

camera:
  fps: ${hardware.mujoco.observation_hz}