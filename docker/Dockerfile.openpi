ARG BASE_IMAGE=positro/openpi-base:latest
FROM ${BASE_IMAGE}

# Set working directory
WORKDIR /positronic

# Copy the positronic repository
COPY . /positronic


# Update Python path to include both projects
ENV PYTHONPATH=/positronic:/openpi

# Default command
CMD ["bash"]
